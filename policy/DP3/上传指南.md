# 上传指南：将 DP3 代码上传到 GitHub

> 目标仓库: https://github.com/zhangzhongbo2213/dual_dp3

---

## 📋 准备工作

### 1. 检查远程仓库配置

```bash
cd /data/zzb/RoboTwin/policy/DP3
git remote -v
```

应该看到：
```
dual_dp3	https://github.com/zhangzhongbo2213/dual_dp3.git (fetch)
dual_dp3	https://github.com/zhangzhongbo2213/dual_dp3.git (push)
```

如果没有，添加远程仓库：
```bash
git remote add dual_dp3 https://github.com/zhangzhongbo2213/dual_dp3.git
```

### 2. 检查 .gitignore

确保 `.gitignore` 文件已正确配置，排除以下内容：
- 数据文件 (`data/`, `scripts/data*/`)
- 检查点文件 (`checkpoints/`)
- 缓存文件 (`__pycache__/`, `*.pyc`)
- 输出文件 (`outputs/`, `wandb/`)

---

## 🚀 方法一：使用上传脚本（推荐）

### 步骤

1. **运行上传脚本**
   ```bash
   cd /data/zzb/RoboTwin/policy/DP3
   bash upload_to_github.sh
   ```

2. **确认上传**
   - 脚本会显示当前状态
   - 输入 `y` 确认上传
   - 等待上传完成

### 脚本功能

- ✅ 自动检查 git 状态
- ✅ 添加所有文件
- ✅ 创建提交信息
- ✅ 推送到 GitHub

---

## 🚀 方法二：手动上传

### 步骤

1. **检查当前状态**
   ```bash
   cd /data/zzb/RoboTwin/policy/DP3
   git status
   ```

2. **添加文件**
   ```bash
   # 添加所有文件（.gitignore 会自动排除不需要的文件）
   git add .
   
   # 或者选择性添加
   git add README.md
   git add docs/
   git add scripts/
   git add 3D-Diffusion-Policy/
   git add inference_*.py
   git add deploy_policy.py
   git add combined_policy.py
   git add train_*.sh
   git add process_data*.sh
   ```

3. **提交更改**
   ```bash
   git commit -m "Update DP3 implementation with EndPose and GNN-EndPose variants

   - Add DP3-EndPose model implementation
   - Add DP3-GNN-EndPose model implementation  
   - Add inference scripts
   - Add deployment scripts
   - Add comprehensive documentation
   - Add training scripts for all model variants
   - Add data processing tools"
   ```

4. **推送到 GitHub**
   ```bash
   # 推送到 main 分支
   git push dual_dp3 main
   
   # 或者创建新分支
   git push -u dual_dp3 main:main
   ```

---

## 📦 需要上传的主要文件

### 核心代码
- ✅ `3D-Diffusion-Policy/` (完整目录，排除 data/)
- ✅ `inference_endpose.py`
- ✅ `inference_gnn_endpose.py`
- ✅ `combined_policy.py`
- ✅ `deploy_policy.py`

### 脚本
- ✅ `scripts/` (完整目录，排除 data*/)
- ✅ `train_endpose.sh`
- ✅ `train_gnn_endpose.sh`
- ✅ `train_gnn_endpose_bowls.sh`
- ✅ `train_endpose_bowls_randomized.sh`
- ✅ `process_data_endpose.sh`
- ✅ `process_data_gnn_endpose.sh`

### 文档
- ✅ `README.md`
- ✅ `docs/` (完整目录)
- ✅ `对比报告_GitHub_vs_本地.md`
- ✅ `上传指南.md`

### 配置文件
- ✅ `.gitignore`
- ✅ `deploy_policy.yml`
- ✅ `setup_hf_mirror.sh`

---

## ❌ 不需要上传的文件

以下文件已在 `.gitignore` 中排除：

- ❌ `data/` - 数据文件（太大）
- ❌ `checkpoints/` - 模型检查点（太大）
- ❌ `scripts/data*/` - 处理后的数据
- ❌ `__pycache__/` - Python 缓存
- ❌ `*.pyc` - 编译文件
- ❌ `wandb/` - 训练日志
- ❌ `outputs/` - 输出文件

---

## 🔍 验证上传

### 1. 检查 GitHub 仓库

访问 https://github.com/zhangzhongbo2213/dual_dp3 确认文件已上传。

### 2. 检查文件列表

确保以下关键文件存在：
- ✅ `README.md`
- ✅ `3D-Diffusion-Policy/`
- ✅ `docs/`
- ✅ `scripts/`
- ✅ `inference_endpose.py`
- ✅ `inference_gnn_endpose.py`

### 3. 检查文件大小

如果某些文件太大，GitHub 可能无法显示。检查：
- 单个文件 < 100MB
- 仓库总大小 < 1GB

---

## ⚠️ 常见问题

### Q1: 推送失败 - 权限错误

**解决方案**:
```bash
# 检查 GitHub 认证
git config --global user.name "your-username"
git config --global user.email "your-email@example.com"

# 使用 SSH 或 Personal Access Token
# SSH: git remote set-url dual_dp3 git@github.com:zhangzhongbo2213/dual_dp3.git
# Token: 在 GitHub Settings > Developer settings > Personal access tokens 创建
```

### Q2: 文件太大无法上传

**解决方案**:
```bash
# 检查大文件
find . -type f -size +50M -not -path "./.git/*"

# 使用 Git LFS (如果需要上传大文件)
git lfs install
git lfs track "*.ckpt"
git lfs track "*.zarr"
```

### Q3: 冲突错误

**解决方案**:
```bash
# 先拉取远程更改
git fetch dual_dp3
git merge dual_dp3/main

# 解决冲突后
git add .
git commit -m "Merge remote changes"
git push dual_dp3 main
```

---

## 📝 提交信息模板

### 主要更新
```
Update DP3 implementation with EndPose and GNN-EndPose variants

- Add DP3-EndPose model implementation
- Add DP3-GNN-EndPose model implementation  
- Add inference scripts (inference_endpose.py, inference_gnn_endpose.py)
- Add deployment scripts (deploy_policy.py, combined_policy.py)
- Add comprehensive documentation in docs/
- Add training scripts for all model variants
- Add data processing tools and utilities
- Update README with detailed usage guide
```

### 小更新
```
Fix: [描述修复的问题]
Add: [描述新增的功能]
Update: [描述更新的内容]
```

---

## 🎯 下一步

上传完成后：

1. **更新 GitHub README**
   - 确保 README.md 中的链接正确
   - 更新快速开始指南

2. **创建 Release**
   - 在 GitHub 上创建标签和发布
   - 添加发布说明

3. **通知团队**
   - 分享仓库链接
   - 说明主要更新内容

---

**最后更新**: 2025年1月  
**版本**: v1.0

